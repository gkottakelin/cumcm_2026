# MODEL V1

## 1 总体建模思路

四个问题可以统一为“带有界角度误差的序贯定位与搜索决策”问题：

1. 问题 1：把每次示向度转换为一个角度可行域，求多个可行域的交集，并计算定位区域的直径与最小包围圆。
2. 问题 2：在第一次测量形成的不确定区域基础上，选择第二个检测点，使第二次测量后最坏情况下的定位误差尽量小。
3. 问题 3：对全向干扰源先建立保证不漏检的空间覆盖，再进行自适应交会定位和路径调度。
4. 问题 4：把干扰源的未知发射方向加入状态空间，将普通空间覆盖扩展为“位置—方向联合覆盖”。

全题建议采用分层目标：

$$
\text{第一层：最大化清除比例；}\qquad
\text{第二层：在全部清除的条件下最小化定位清除总时间。}
$$

不能把平均定位清除时间作为唯一目标，因为少清除若干目标也可能得到较短的平均时间，但这不符合“确保所有干扰源被清除”的要求。

---

## 2 符号说明与基本假设

| 符号 | 含义 |
|---|---|
| $\Omega$ | 半径为 1800 m 的目标圆域，$\Omega=\{x\in\mathbb R^2:\|x\|_2\le1800\}$ |
| $s_i$ | 第 $i$ 个检测点坐标 |
| $g$ | 某干扰源的真实位置 |
| $\theta_i$ | 在检测点 $s_i$ 处测得的示向度 |
| $\delta$ | 示向误差上界，本题 $\delta=1^\circ$ |
| $W_i$ | 第 $i$ 次测量对应的示向角度区域 |
| $P_k$ | 前 $k$ 次有效示向测量后的定位可行域 |
| $D(P)$ | 定位区域 $P$ 的直径 |
| $R(P)$ | 覆盖 $P$ 的最小圆半径 |
| $r_j$ | 第 $j$ 个干扰源的有效接收半径，$1000\le r_j\le1500$ |
| $v$ | 机器狗移动速度，$v=5\text{ m/s}$ |
| $z_j$ | 频道 $j$ 的信息状态，包括历史测量、定位区域及是否清除 |

建议作如下假设：

- 干扰源在一次测试中保持静止，频道与有效接收半径不变。
- 机器狗的坐标和移动没有误差，定位误差只来自示向度的 $\pm1^\circ$ 误差。
- 同一地点的测量误差固定，因此同一点重复测量不能通过平均消除误差。
- 目标位于二维平面中，所有距离均采用欧氏距离。
- 角度边界、接收距离边界和目标区域边界均包含边界点。
- 全向源和定向源在有效方向范围内的检测规律均服从题面给出的确定性判据。

---

## 3 问题一的定位区域模型

### 3.1 单次示向度的角度带

定义单位方向向量

$$
u(\alpha)=
\begin{bmatrix}
\cos\alpha\\
\sin\alpha
\end{bmatrix},
$$

二维向量叉积定义为

$$
a\times b=a_xb_y-a_yb_x.
$$

在检测点 $s_i$ 测得示向度 $\theta_i$ 后，真实方位角位于

$$
[\theta_i-\delta,\theta_i+\delta].
$$

当角区间宽度小于 $180^\circ$ 时，候选点 $x$ 属于该示向锥，当且仅当

$$
u(\theta_i-\delta)\times(x-s_i)\ge0,
$$

$$
u(\theta_i+\delta)\times(x-s_i)\le0.
$$

因此，一次测量对应两个线性半平面约束。记该角度带为

$$
W_i=\left\{x:
u(\theta_i-\delta)\times(x-s_i)\ge0,
u(\theta_i+\delta)\times(x-s_i)\le0
\right\}.
$$

若有 $m$ 个检测点，则干扰源定位区域为

$$
P=\Omega\cap\bigcap_{i=1}^{m}W_i.
$$

每个 $W_i$ 都是凸集，圆域 $\Omega$ 也是凸集，所以 $P$ 必为凸集。若将圆域边界用高密度正多边形近似，则可通过逐次半平面裁剪得到凸多边形定位区域。

### 3.2 定位区域直径

设定位多边形的顶点依逆时针排列为 $v_1,v_2,\ldots,v_n$，则区域直径为

$$
D(P)=\max_{1\le a<b\le n}\|v_a-v_b\|_2.
$$

由于 $P$ 为凸集，最大距离一定在边界顶点之间取得。可采用两种算法：

- 顶点数较少时，枚举全部顶点对，复杂度为 $O(n^2)$；
- 先求凸包，再用旋转卡壳搜索对踵点，复杂度为 $O(n)$。

必须单独处理以下退化情况：

- 约束相互矛盾，定位区域为空；
- 定位区域退化为线段或单点；
- 两条测向线接近平行，导致定位区域狭长且直径很大；
- 角度跨越 $0^\circ$，例如区间 $[359^\circ,1^\circ]$，需要统一角度表示。

### 3.3 以定位区域直径为直径的圆能否覆盖定位区域

仅知道集合直径为 $D$，不能断言半径为 $D/2$ 的圆一定能够覆盖该集合。例如，边长为 $D$ 的等边三角形，其最小外接圆半径为

$$
\frac{D}{\sqrt3}>\frac D2.
$$

因此，应计算定位区域的最小包围圆：

$$
R(P)=\min_c\max_{x\in P}\|x-c\|_2.
$$

对于凸多边形，只需考虑其顶点。最小包围圆由以下两种边界集之一确定：

- 两个顶点构成圆的直径；
- 三个顶点共同确定外接圆。

可以采用 Welzl 随机增量算法求解，期望复杂度为 $O(n)$。问题一的判据为

$$
\boxed{
\text{存在一个直径为 }D(P)\text{ 的圆覆盖 }P
\iff R(P)\le \frac{D(P)}2
}
$$

如果题目将圆心指定为某一对直径端点的中点，则还需要直接检验所有顶点到该中点的距离是否均不超过 $D(P)/2$。

### 3.4 问题一建议绘图

**图 1 示向锥与定位区域示意图**

- 画出两个或三个检测点；
- 每个检测点画测量中心线和 $\pm1^\circ$ 两条边界线；
- 用浅色填充各示向锥，用深色突出交集 $P$；
- 标出最远顶点对及距离 $D(P)$；
- 叠加最小包围圆并标注 $R(P)$。

这张图承担问题一的大部分几何解释，建议作为正文核心图。

**图 2 交会角对定位区域的影响**

- 左图：两条中心线夹角约 $15^\circ$；
- 右图：两条中心线夹角约 $90^\circ$；
- 两图使用相同角度误差和近似相同目标距离；
- 对比定位区域的长度、面积和直径。

该图可自然引出问题二的第二检测点选择。

---

## 4 问题二的第二检测点选择模型

### 4.1 极小极大选点模型

第一次测量后得到定位可行域 $P_1$。若把第二检测点设为 $s$，而真实目标可能位于任意 $g\in P_1$，由 $g$ 产生的第二次示向角度带记为 $W(s,g)$。

定义第二次测量后的最坏定位直径：

$$
F(s)=\max_{g\in P_1}D\bigl(P_1\cap W(s,g)\bigr).
$$

第二检测点可通过以下模型选择：

$$
s_2^*=\arg\min_{s\in C}
\left[
F(s)+\lambda\frac{\|s-s_1\|_2}{v}
\right].
$$

其中：

- $C$ 为允许的候选区域；
- 第一项衡量最坏定位精度；
- 第二项衡量移动时间；
- $\lambda$ 是定位精度和移动代价之间的权重。

问题二只强调定位效果时可令 $\lambda=0$。在问题三、四的动态策略中应保留移动代价项。

### 4.2 交会角与 Fisher 信息

设两条真实视线的夹角为 $\beta$。当 $\beta$ 接近 $0^\circ$ 或 $180^\circ$ 时，两条测向线接近平行，位置误差被明显放大。当两次观测距离接近时，$\beta$ 接近 $90^\circ$ 通常具有较好的定位几何。

在小误差近似下，方位测量的 Fisher 信息矩阵可写为

$$
I(g)=\sum_i
\frac{1}{\sigma^2\|g-s_i\|_2^2}
n_in_i^\mathsf T,
$$

其中

$$
n_i=
\begin{bmatrix}
-\sin\alpha_i\\
\cos\alpha_i
\end{bmatrix}.
$$

候选点的评价指标可以选择：

- D 最优：最大化 $\det I(g)$；
- A 最优：最小化 $\operatorname{tr}(I^{-1}(g))$；
- E 最优：最大化 $\lambda_{\min}(I(g))$；
- 本题几何指标：最小化最坏定位区域直径 $F(s)$。

前三个指标适合解释传感器几何，最后一个指标与题目给定的有界角度误差更一致，建议作为主模型。

### 4.3 第二检测点候选区域

令

$$
u=u(\theta_1),\qquad
n=(-\sin\theta_1,\cos\theta_1)^\mathsf T,
$$

把候选点写成

$$
s=s_1+a u+b n.
$$

候选区域应满足：

1. 与第一次检测点保持足够基线长度；
2. 对 $P_1$ 中的代表性目标点，第二视线和第一视线的夹角优先落在 $60^\circ$ 至 $120^\circ$；
3. 到代表性目标点的距离尽量不超过 1000 m，以提高必然收到信号的可能性；
4. 同时保留首次示向线左右两侧的候选区域，比较两侧的移动距离和最坏定位直径；
5. 排除会造成两条测向线近似平行的区域。

需要注意：第一次能收到信号只说明目标与 $s_1$ 的距离不超过该目标的实际接收半径，而实际半径在 $[1000,1500]$ 内未知。因而，无法对所有目标位置都保证第二点仍然收到信号时，应把“可接收概率”或“最坏接收缺口”加入候选点评价，而不能直接假定接收半径恒为 1500 m。

### 4.4 问题二建议绘图

**图 3 第二检测点候选区域图**

- 画出第一次示向锥 $P_1$；
- 画出首次示向方向和垂直方向；
- 用颜色表示每个候选点的 $F(s)$；
- 叠加 $60^\circ$ 至 $120^\circ$ 交会角可行区；
- 标出最优点 $s_2^*$。

推荐使用二维热力图。颜色越深表示最坏定位直径越小。

**图 4 交会角与定位误差关系曲线**

- 横轴：交会角 $\beta$，范围可取 $5^\circ$ 至 $175^\circ$；
- 纵轴：定位区域直径、面积或 CRLB；
- 设置若干目标距离作为多条曲线；
- 预期在接近 $90^\circ$ 时误差较小，在接近 $0^\circ$ 和 $180^\circ$ 时快速增大。

---

## 5 问题三的全向源搜索定位与清除模型

### 5.1 总时间模型

总虚拟时间由移动、频道切换、检测和清除组成：

$$
T_{\mathrm{total}}
=\frac{L_{\mathrm{move}}}{5}
+5N_{\mathrm{measure}}
+N_{\mathrm{switch}}
+3N_{\mathrm{clear,fail}}
+5N_{\mathrm{clear,success}}.
$$

题目要求的平均定位清除时间为

$$
\bar T=\frac{T_{\mathrm{total}}}{N_{\mathrm{clear}}}.
$$

建议把问题三拆成三层：

- 覆盖层：保证每个全向源至少被检测一次；
- 定位层：逐步缩小各频道的定位可行域；
- 调度层：降低覆盖点、追加测量点和清除点之间的移动时间。

### 5.2 七点严格覆盖方案

全向源的有效接收半径至少为 1000 m。只要一组检测点的 1000 m 圆盘能够覆盖整个目标圆域，逐频道扫描这些检测点就能保证每个未清除全向源至少被检测一次。

构造一个中心点和六个外圈点：

$$
S_0=\{(0,0)\}\cup
\left\{
1150
\begin{bmatrix}
\cos(k\pi/3)\\
\sin(k\pi/3)
\end{bmatrix}
:k=0,1,\ldots,5
\right\}.
$$

对任意目标点 $x$：

- 若 $\|x\|\le1000$，则中心检测点可以检测到该目标；
- 若 $1000\le\rho=\|x\|\le1800$，必存在一个外圈检测点，其极角与 $x$ 的极角差满足 $|\alpha|\le30^\circ$。

目标与该外圈检测点的距离满足

$$
d^2=\rho^2+1150^2-2\rho\cdot1150\cos\alpha.
$$

在 $\rho\in[1000,1800]$、$|\alpha|\le30^\circ$ 内，最大值在边界处取得，且

$$
\max\{d(1000,30^\circ),d(1800,30^\circ)\}<1000.
$$

因此七个检测点的 1000 m 圆盘严格覆盖目标圆域。这一方案提供“不会因为距离过远而漏掉全向源”的证明。

外圈半径 1150 m 是一个便于证明的可行值，不必宣称为全局最优。可进一步把外圈半径 $r$ 作为变量，在满足覆盖约束的前提下最小化巡回路径长度。

### 5.3 序贯定位更新

对每个频道维护定位区域 $P_k$。第一次收到 `direction` 后：

$$
P_1=\Omega\cap W_1.
$$

每获得一次新的示向度，更新为

$$
P_k=P_{k-1}\cap W_k.
$$

下一检测点采用问题二的极小极大准则，并考虑当前位置到候选点的移动时间。

当

$$
R(P_k)\le20-\varepsilon
$$

时，以最小包围圆圆心为清除点。$\varepsilon>0$ 是数值与模型安全裕量。相比仅要求 $D(P_k)\le40$，最小包围圆条件能够直接保证定位区域中的每一点都位于清除半径内。

若检测结果为 `near`，题面已经保证目标距离不超过 5 m，可直接在当前点清除。

如果追加检测返回 `no_signal`，在全向源假设下，由于接收半径至少为 1000 m，可以安全推出

$$
\|g-s\|>1000.
$$

因而可以更新

$$
P\leftarrow P\cap\{x:\|x-s\|>1000\}.
$$

不能用 1500 m 作为排除半径，因为干扰源的实际接收半径可能小于 1500 m。

### 5.4 动态路径调度

覆盖点、追加测量点和清除点共同组成动态任务集合。可先为七个覆盖点规划一条短巡回路线；当某频道达到清除条件时，把清除点插入当前剩余路线中增量最小的位置。

若候选动作点为 $q$，可以定义单位时间信息收益：

$$
\operatorname{score}(q)=
\frac{\Delta U(q)}
{\|q-c\|/5+5+\mathbf1_{\{\text{频道切换}\}}},
$$

其中 $\Delta U(q)$ 可取：

- 定位区域最坏直径的下降量；
- 定位区域面积的下降量；
- $\log\det I$ 的增量。

频道切换只耗时 1 s，而移动数百米可能耗时几十至几百秒。因此，减少移动通常比减少一次频道切换更重要。

### 5.5 问题三建议绘图

**图 5 七点覆盖证明图**

- 画出半径 1800 m 的目标圆；
- 标出中心点和半径 1150 m 的六个外圈点；
- 以每个检测点为圆心画半径 1000 m 的覆盖圆；
- 突出目标圆边界上最不利的六类中间点。

**图 6 某频道定位区域递推收缩图**

- 分别绘制 $P_1,P_2,P_3$；
- 标注每一步的 $D(P_k)$ 和 $R(P_k)$；
- 用相同坐标轴展示定位区域如何缩小到半径 20 m 内。

**图 7 机器狗搜索与清除轨迹图**

- 画目标圆、覆盖点、实际访问顺序和已清除目标；
- 用不同点型表示普通扫描点、追加测量点、清除点；
- 在线段上用箭头表示移动方向；
- 不建议把所有频道的示向线全部画在一张图上，容易失去可读性。

**图 8 总时间构成图**

- 使用堆积柱状图；
- 每根柱对应一个演练案例或一种策略；
- 分段表示移动、检测、频道切换、成功清除和失败清除耗时。

---

## 6 问题四的定向源模型

### 6.1 定向源检测条件

设干扰源位置为 $g$，发射中心方向为 $\phi$，方向向量为

$$
d(\phi)=(\cos\phi,\sin\phi)^\mathsf T.
$$

检测点 $s$ 能接收到该定向源，当且仅当

$$
\|s-g\|_2\le r_g,
$$

$$
d(\phi)^\mathsf T(s-g)\ge0.
$$

第二个条件表示检测点位于以 $g$ 为边界、法向为 $d(\phi)$ 的发射半平面内。

因此，问题四中的 `no_signal` 可能由三种原因产生：

1. 该频道没有未清除干扰源；
2. 检测点距离目标过远；
3. 检测点位于定向源发射方向的背面。

所以问题三中“无信号就排除 1000 m 圆盘”的更新规则不能直接用于问题四。

### 6.2 位置方向联合状态空间

把一个潜在定向源状态写为

$$
\omega=(g,\phi),qquad g\in\Omega,\quad\phi\in[0,2\pi).
$$

给定候选检测点集合

$$
Q=\{q_1,q_2,\ldots,q_M\},
$$

定义覆盖矩阵

$$
A_{\omega j}
=\mathbf1\left\{
\|q_j-g\|\le1000,
\ d(\phi)^\mathsf T(q_j-g)\ge0
\right\}.
$$

选择检测点可以建模为加权集合覆盖问题：

$$
\min\sum_{j=1}^M w_jy_j,
$$

$$
\text{s.t.}\quad
\sum_{j=1}^M A_{\omega j}y_j\ge1,
\qquad \forall\omega,
$$

$$
y_j\in\{0,1\}.
$$

权重 $w_j$ 可以综合检测点数量、预计移动增量和扫描频道数量。

连续状态空间需要离散化。为避免离散网格出现虚假的“完全覆盖”，优化时应采用收紧约束：

$$
\|q_j-g\|\le1000-\eta_r,
$$

$$
d(\phi)^\mathsf T(q_j-g)\ge\eta_a,
$$

其中 $\eta_r$ 和 $\eta_a$ 是距离与方向安全裕量。求解后，再在更密且独立的验证网格上检查未覆盖状态比例。

### 6.3 集合信念更新

对频道 $j$ 建立信念集合 $B_j$，其元素包含可能位置、源类型和可能的定向方向。检测结果到达后，执行一致性更新：

$$
B_j^+=\{\omega\in B_j:h(\omega,q)=o\},
$$

其中 $q$ 是检测点，$o$ 是实际观测，$h$ 是观测判定函数。

- 收到 `direction`：位置必须落在示向锥内，同时检测点应位于该状态的发射半平面内；
- 收到 `no_signal`：删除所有在该检测点理应产生信号的状态；
- 收到 `near`：目标距离不超过 5 m，可直接尝试清除；
- 清除成功：对应频道从后续扫描任务中移除。

不要过早把频道硬分类为全向或定向。证据不足时同时保留两类假设，可以避免把定向源背面的无信号误判为频道不存在。

### 6.4 下一动作选择

每一步从“扫描未覆盖状态、追加定位测量、执行清除”三类动作中选择。可定义

$$
q^*=\arg\max_{q\in Q_{\mathrm{rem}}}
\frac{
aC_{\mathrm{new}}(q)+b\Delta U(q)
}
{
\|q-c\|/5+5N_{\mathrm{scan}}(q)+N_{\mathrm{switch}}(q)
}.
$$

其中：

- $C_{\mathrm{new}}(q)$：该点新覆盖的位置—方向状态数；
- $\Delta U(q)$：对已发现目标的定位不确定性下降量；
- $a,b$：覆盖和定位的权重；
- 分母：完成该动作所需的预计时间。

建议采用以下动作优先级：

1. 若某频道已满足 $R(P)\le20-\varepsilon$，比较立即清除和顺路清除的增量时间；
2. 若已发现频道仍不满足清除条件，选择保持可接收且具有较大交会角的追加测量点；
3. 若仍存在未覆盖的位置—方向状态，前往新增覆盖量最大的检测点；
4. 已清除 16 个目标时可立即结束；若清除数少于 16，则必须完成预定覆盖证书后才能判定不存在剩余目标。

### 6.5 问题四建议绘图

**图 9 定向源可检测区域示意图**

- 画出源位置、半径为 $r_g$ 的接收圆；
- 用半圆阴影表示 $180^\circ$ 发射范围；
- 标出同距离下一个可检测点和一个背面不可检测点；
- 用这张图解释 `no_signal` 的多义性。

**图 10 位置方向联合覆盖图**

位置和方向共三维，不适合直接画三维散点。建议固定若干方向 $\phi$，分别画对应的空间覆盖热力图：

- 横纵轴为目标位置 $(x,y)$；
- 颜色表示能覆盖该状态的已选检测点数量；
- 分面展示 $\phi=0^\circ,45^\circ,90^\circ,\ldots$；
- 未覆盖区域应为空。

**图 11 全向方案与定向鲁棒方案对比图**

- 横轴：策略；
- 纵轴：清除比例或漏检率；
- 分别给出纯全向案例、混合案例；
- 说明七点全向覆盖不能保证定向源被发现。

---

## 7 敏感性分析方案

### 7.1 必做参数

| 参数 | 建议取值 | 主要观察指标 | 目的 |
|---|---:|---|---|
| 安全清除裕量 $\varepsilon$ | 0、1、2、3、5 m | 清除失败率、平均时间、追加测量次数 | 判断精度与效率的权衡 |
| 定位与移动权重 $\lambda$ | 0、0.1、0.5、1、2、5 | 最坏定位直径、移动距离、总时间 | 验证第二检测点目标函数稳定性 |
| 候选点空间网格间距 $h$ | 25、50、100、150 m | 最优目标值、运行时间、未覆盖率 | 判断离散精度是否足够 |
| 定向角离散步长 $\Delta\phi$ | 2°、5°、10°、15° | 独立密网格未覆盖率、检测点数量 | 防止粗网格漏掉最坏方向 |
| 距离安全裕量 $\eta_r$ | 0、20、50、100 m | 鲁棒覆盖率、检测点数量、路线长度 | 评价集合覆盖的保守程度 |
| 交会角下限 $\beta_{\min}$ | 30°、45°、60°、75° | 定位直径、追加测量次数、移动时间 | 验证推荐交会角区间 |
| 外圈半径 $r$ | 1100 至 1250 m，步长 25 m | 最坏覆盖距离、巡回长度 | 优化七点覆盖构造 |

### 7.2 安全清除裕量分析

以 $\varepsilon$ 为横轴，同时画：

- 清除失败率；
- 平均追加测量次数；
- 平均定位清除时间。

预期规律是：$\varepsilon$ 增大后清除失败率下降，但追加测量次数和总时间上升。选择拐点处的 $\varepsilon$，而不是仅选择使平均时间最短的值。

### 7.3 网格收敛分析

空间网格从粗到细变化时，记录：

$$
J(h),\quad N_{\mathrm{point}}(h),\quad
\rho_{\mathrm{uncovered}}(h),\quad t_{\mathrm{solve}}(h).
$$

其中 $J(h)$ 为目标函数值，$N_{\mathrm{point}}$ 为选择的检测点数量，$\rho_{\mathrm{uncovered}}$ 为独立验证网格中的未覆盖状态比例。

建议绘制双纵轴图：

- 横轴为空间网格间距 $h$；
- 左轴为目标值或检测点数量；
- 右轴为未覆盖率；
- 当继续加密网格时结果变化很小且未覆盖率保持为 0，可认为离散模型收敛。

方向网格 $\Delta\phi$ 也采用相同方法分析。空间网格和方向网格最好分开变化，避免无法判断误差来源。

### 7.4 示向误差敏感性

题目固定误差上界为 $1^\circ$，但可额外考察模型在不同测向精度下的表现：

$$
\delta\in\{0.25^\circ,0.5^\circ,1^\circ,1.5^\circ,2^\circ\}.
$$

观察：

- 第一次定位区域面积；
- 两次测量后的定位直径；
- 达到 $R(P)\le20-\varepsilon$ 所需测量次数；
- 平均定位清除时间。

这项分析不是为了修改题目参数，而是说明模型对测向精度变化的适应性。

### 7.5 接收半径敏感性

对全向源，可让实际接收半径在 $[1000,1500]$ 内取不同分布：

- 全部取 1000 m，构造最坏情况；
- 在 $[1000,1500]$ 上均匀分布；
- 集中在 1000 m 附近；
- 集中在 1500 m 附近。

比较七点保底覆盖与利用较大接收半径减少扫描点的自适应策略。论文结论应强调：七点证明只使用下界 1000 m，因此不依赖具体分布；利用较大接收半径的策略只能作为效率改进，不能取代保底覆盖证明。

### 7.6 权重敏感性

对于

$$
\operatorname{score}(q)=
\frac{aC_{\mathrm{new}}(q)+b\Delta U(q)}{T(q)},
$$

不要只给出一组 $a,b$。令比值 $a/b$ 在若干数量级上变化，比较：

- 漏检率；
- 平均定位清除时间；
- 总移动距离；
- 已发现但未及时清除的频道数。

若某一较宽区间内结果接近，说明策略对权重选择不敏感；若只有很窄区间表现良好，应在论文中承认策略依赖参数标定。

---

## 8 模型验证与对照实验

### 8.1 几何模型验证

随机生成目标位置和多个检测点，并在 $[-1^\circ,1^\circ]$ 内生成测向误差。每次更新后验证：

$$
g\in P_k.
$$

若真实位置被排除，说明角度方向、叉积符号或跨 $0^\circ$ 处理存在错误。

### 8.2 最小包围圆验证

构造三类人工定位区域：

- 线段；
- 钝角三角形；
- 等边三角形。

验证算法能区分“由两个点决定的最小圆”和“由三个点决定的最小圆”，并用等边三角形说明 $D(P)\le40$ 不必然推出半径 20 m 的圆能够覆盖。

### 8.3 覆盖模型验证

对全向七点方案：

- 在目标圆内使用高密度网格；
- 计算每个网格点到最近检测点的距离；
- 报告最大最近距离；
- 最大值应小于 1000 m。

对定向联合覆盖：

- 使用比建模网格更密的位置网格和方向网格；
- 计算每个状态被多少个检测点覆盖；
- 报告最小覆盖次数和未覆盖比例；
- 若最小覆盖次数仅为 1，可进一步讨论检测点失效或边界误差下的风险。

### 8.4 建议对照组

| 对照内容 | 基线方法 | 改进方法 | 主要指标 |
|---|---|---|---|
| 第二检测点 | 沿首次示向方向移动 | 极小极大或信息增益选点 | 定位直径、测量次数 |
| 全向搜索 | 规则栅格扫描 | 七点严格覆盖 | 路线长度、检测次数 |
| 清除停止 | 仅检查 $D(P)\le40$ | 检查 $R(P)\le20-\varepsilon$ | 清除失败率 |
| 混合源搜索 | 复用全向七点路线 | 位置—方向鲁棒覆盖 | 漏检率、总时间 |
| 清除调度 | 全部搜索后统一清除 | 动态插入清除 | 移动距离、总时间 |

---

## 9 论文结果表建议

### 9.1 问题一结果表

| 检测点数 | 定位区域顶点数 | 面积 | 直径 $D(P)$ | 最小包围圆半径 $R(P)$ | $R(P)\le D(P)/2$ |
|---:|---:|---:|---:|---:|---|

### 9.2 问题二候选点比较表

| 方案 | 第二检测点 | 移动距离 | 最小交会角 | 最坏定位直径 | 定位区域面积 |
|---|---|---:|---:|---:|---:|

### 9.3 问题三和问题四演练统计表

| 策略 | 测试次数 | 平均清除比例 | 最坏清除比例 | 平均定位清除时间 | 平均移动距离 | 平均检测次数 |
|---|---:|---:|---:|---:|---:|---:|

只报告平均值不够，至少同时给出标准差和最坏结果。正式测试只有三次，适合按题目表格逐次列出；策略有效性的统计论证应主要来自次数更多的演练测试。

---

## 10 建议的论文结构

1. 问题重述与分析
2. 模型假设与符号说明
3. 问题一的有界误差交会定位模型
4. 问题二的第二检测点极小极大选择模型
5. 问题三的全向源覆盖定位与动态调度模型
6. 问题四的位置方向联合鲁棒覆盖模型
7. 模型验证、对照实验与敏感性分析
8. 模型评价与改进方向

建议贯穿全文使用同一组核心量：

$$
P_k,\quad D(P_k),\quad R(P_k),\quad
L_{\mathrm{move}},\quad N_{\mathrm{measure}},\quad T_{\mathrm{total}}.
$$

这样四问不会显得是四套互不相关的方法。

---

## 11 建模组需要交付论文组的材料

| 问题 | 至少交付的数据和图 |
|---|---|
| 问题 1 | 定位区域顶点、面积、$D(P)$、$R(P)$、最小包围圆圆心、示向锥交会图 |
| 问题 2 | 候选区域、最优第二检测点、交会角、移动距离、最坏定位直径、候选点热力图 |
| 问题 3 | 七点坐标和覆盖证明、搜索轨迹、各次演练统计、总时间分解、定位区域收缩图 |
| 问题 4 | 位置与方向网格参数、安全裕量、独立验证覆盖率、全向基线与鲁棒策略对照结果 |
| 敏感性分析 | 参数取值表、均值和最坏值、误差棒、网格收敛曲线及参数选择依据 |

---

## 12 参考文献

1. 全国大学生数学建模竞赛组委会. 2026 年高教社杯全国大学生数学建模竞赛 B 题：无线电干扰源的快速自动定位与清除. 2026.
2. Gavish M, Weiss A J. Performance analysis of bearing-only target location algorithms. *IEEE Transactions on Aerospace and Electronic Systems*, 1992, 28(3): 817–828.
3. Doğançay K, Hmam H. Optimal angular sensor separation for AOA localization. *Signal Processing*, 2008, 88(5): 1248–1260. [DOI](https://doi.org/10.1016/j.sigpro.2007.11.013)
4. Bishop A N, Fidan B, Anderson B D O, Doğançay K, Pathirana P N. Optimality analysis of sensor-target localization geometries. *Automatica*, 2010, 46(3): 479–492. [DOI](https://doi.org/10.1016/j.automatica.2009.12.003)
5. Welzl E. Smallest enclosing disks balls and ellipsoids. In: *New Results and New Trends in Computer Science*. LNCS 555. Springer, 1991: 359–370. [DOI](https://doi.org/10.1007/BFb0038202)
6. Choset H. Coverage for robotics: A survey of recent results. *Annals of Mathematics and Artificial Intelligence*, 2001, 31: 113–126. [DOI](https://doi.org/10.1023/A:1016639210559)
7. Galceran E, Carreras M. A survey on coverage path planning for robotics. *Robotics and Autonomous Systems*, 2013, 61(12): 1258–1276. [DOI](https://doi.org/10.1016/j.robot.2013.09.004)
8. Kaelbling L P, Littman M L, Cassandra A R. Planning and acting in partially observable stochastic domains. *Artificial Intelligence*, 1998, 101(1–2): 99–134. [DOI](https://doi.org/10.1016/S0004-3702%2898%2900023-X)
9. Zhao S, Chen B M, Lee T H. Optimal sensor placement for target localization and tracking in 2D and 3D. *International Journal of Control*, 2013, 86(10): 1687–1704. [DOI](https://doi.org/10.1080/00207179.2013.792605)
